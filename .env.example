# ——— Ubuntu song server (required for Vercel deploys) ———
# Public base of the media-server (used by Next to list/delete + mint upload tokens)
MEDIA_API_URL=https://media.example.com
MEDIA_API_SECRET=change-me-to-a-long-random-string

# ——— Vercel room/queue state (songs stay on Ubuntu) ———
# Create a Blob store in the Vercel dashboard, then redeploy.
# BLOB_READ_WRITE_TOKEN=
