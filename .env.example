# Ubuntu is the only storage you need.
# Same values on media-server/.env and Vercel env vars.

MEDIA_API_URL=https://YOUR-SUBDOMAIN.ngrok-free.app
MEDIA_API_SECRET=change-me-to-a-long-random-string

# Blob is optional and NOT required when MEDIA_API_URL is set.
# BLOB_READ_WRITE_TOKEN=
